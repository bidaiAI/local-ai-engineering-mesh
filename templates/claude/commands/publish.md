# /publish

Use this command before commit, push, PR, or deploy.

Required checks:
- changed files understood
- no secret leakage
- verification already passed
- branch / PR narrative matches reality
- release risk is stated explicitly

Publishing is a separate decision from implementation.
