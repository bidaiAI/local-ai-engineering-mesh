---
description: 上下文纪律 — 对话管理、噪声隔离与信息提取效率
alwaysApply: true
---

# Context Discipline

## 信息提取效率

- 只获取「刚好需要」的信息。读文件时用 offset/limit 定位目标区域，而非全文读取超长文件。
- 搜索时用精确模式（Grep 精确匹配 > Glob 模式 > 全目录扫描），避免把大量无关结果灌入上下文。
- 已读过的文件不要重复读取，已搜索过的模式不要重复搜索。记住已获取的上下文。

## 噪声隔离

- 运行测试、分析日志、大范围搜索等产生大量输出的操作，优先通过 Task 工具委派给子 Agent，只接收摘要结果。
- 命令输出超过必要时，指定 head_limit 或使用管道截取关键部分。
- 不要在主对话中堆积大段原始输出。提炼要点，引用关键行。

## 对话卫生

- 一件事做完再做下一件。避免在同一轮中混杂多个不相关任务的上下文。
- 长对话后如果主题发生重大转换，主动建议用户开新对话以保持上下文质量。
- 回复中不要重复已经展示过的信息。指向之前的引用即可。

## 上下文压缩时机

安全压缩：调研完成后、里程碑达成后、调试结束后、尝试失败换方向后。
危险压缩：实现进行中 — 会丢失变量名、文件路径、局部状态，导致后续操作出错。

## 用户意图推断

指令有模糊和精确两种模式，对应不同的响应策略：
- **模糊指令**（"这个文件有什么可以改进的？"）→ 探索性回应，列出发现和建议，不直接动手。
- **精确指令**（"在第42行加 debounce 300ms"）→ 直接执行，不要过度讨论。
- **判断标准**：如果任务的实现方式只有一种，直接做。如果有多种选择，先讨论或规划。
