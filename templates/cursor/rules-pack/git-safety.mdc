---
description: Git 安全纪律 — 版本控制铁律与分支管理
alwaysApply: true
---

# Git Safety

## 铁律

1. **永远不要自动 push** — commit 可以做，push 必须由用户确认。push 到远端后回退成本是本地的十倍。
2. **频繁 commit** — 完成一个逻辑单元就 commit。小颗粒 commit 让回退精确、让 diff 可读。
3. **禁止破坏性操作** — `git reset --hard`、`git push --force`、`rm -rf` 必须明确警告用户后果，等待确认后执行。
4. **不要修改 git config** — 不要改用户名、邮箱、别名等全局或项目 git 配置。

## Commit 纪律

- 消息说「为什么」而非「做了什么」。diff 已经说明了做了什么。
- 不要 commit 包含 secrets 的文件（.env, credentials.json, API keys）。发现时警告用户。
- 如果 pre-commit hook 失败，修复问题后创建新 commit，不要 `--amend` 除非满足所有 amend 条件。

## 分支管理

- 探索性或风险性修改：建议用户在新分支上操作。
- PR 描述用完整句子写清「做了什么、为什么」，不要只列文件名或标识符。
- 永远不要 force push 到 main/master。
