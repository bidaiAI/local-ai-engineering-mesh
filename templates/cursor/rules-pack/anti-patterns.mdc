---
description: 反模式防御 — 过度工程、范围膨胀、API 幻觉的识别与预防
alwaysApply: true
---

# Anti-Pattern Defense

## 过度工程（Over-Engineering）

识别信号：你正在为一个简单需求引入泛型、接口、工厂模式、抽象层。

铁律：
- 用户要工具函数，不要给抽象框架。优先用简单方案，抽象的引入必须有第二个使用者。
- 如果一个功能用 20 行能写完，不要写成 200 行。杀鸡不用牛刀。
- 不要预测未来需求。YAGNI — 没有明确需要的抽象就是负债。

## 范围膨胀（Scope Creep）

铁律：
- 只改任务要求改的代码。不做 drive-by 重构、不顺手优化、不改无关文件。
- 如果发现相邻问题值得修复，**报告**给用户而非自作主张修改。
- 修一个函数时，不要重构整个文件。改动范围必须可预测。

## API 幻觉（Hallucinated APIs）

高风险场景：小众库的新版本、刚发布的框架 API、不常见的配置项。

防御策略：
- 不确定 API 是否存在时，先读项目依赖的版本号，再搜索实际的类型定义或源码。
- 使用已在项目中验证过的 API 模式，而非凭记忆构造调用。
- 给出代码后，主动说明哪些 API 调用需要用户确认（尤其是第三方库）。

## 过度热心（Overly Helpful）

铁律：
- 用户让修 bug，不要顺手重命名变量、调整格式、添加注释。
- 不要在用户没要求时添加文档文件。
- 不要在回复中推销后续任务。如果有明显的后续步骤，简短提一句即可。
